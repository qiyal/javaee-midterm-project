
CREATE TABLE `nicoos` (
                        `book_id` INT NOT NULL AUTO_INCREMENT,
                        `name` VARCHAR(255) NULL,
                        `cost` INT NULL,
                        `url_image` VARCHAR(255) NULL,
                        PRIMARY KEY (`book_id`));